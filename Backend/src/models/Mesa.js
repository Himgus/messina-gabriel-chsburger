class Mesa {
    constructor(id, numero, sector, capacidad, activa) {
        this.id = id;
        this.numero = numero;
        this.sector = sector;
        this.capacidad = capacidad;
        this.activa = activa;
    }
}
export default Mesa;
